# Secrets — never commit your Roboflow API key
.env
*.env

# Model weights (never commit large weight files)
*.pt
models/*.pt
models/*.onnx

# Roboflow dataset exports + local training runs (large, reproducible from Roboflow)
datasets/
runs/

# Training console output
runs_train*.log
runs_train*.err

# Roboflow workflow image outputs written to disk
rf_*.jpg
calibration.jpg

# Python
__pycache__/
*.pyc
.pytest_cache/

# Virtual environments
.venv/
.venv-stream/
venv/
